{
  "scripts": {
    "format": "prettier . --write",
    "test": "playwright test",
    "test:ui": "playwright test --ui-host=localhost"
  },
  "devDependencies": {
    "@playwright/test": "^1.44.1",
    "@types/node": "^20.12.13",
    "prettier": "^3.2.5"
  }
}
